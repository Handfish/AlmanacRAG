# Local state holds Neon/Gemini secrets in plaintext — never commit it.
*.tfstate
*.tfstate.*
.terraform/
.terraform.lock.hcl.bak

# Secret var files.
terraform.tfvars
*.auto.tfvars

# Crash logs / plans.
crash.log
*.tfplan
